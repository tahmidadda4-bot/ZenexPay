import 'package:flutter/material.dart';
import '../services/supabase_service.dart';
class AchievementsPage extends StatefulWidget{const AchievementsPage({super.key});@override State<AchievementsPage> createState()=>_AchievementsPageState();}
class _AchievementsPageState extends State<AchievementsPage>{late Future<UserAnalytics> future;@override void initState(){super.initState();future=SupabaseService.analytics();}@override Widget build(BuildContext c)=>Scaffold(appBar:AppBar(title:const Text('Level & Achievements',style:TextStyle(fontWeight:FontWeight.w900)),actions:[IconButton(onPressed:()=>setState(()=>future=SupabaseService.analytics()),icon:const Icon(Icons.refresh_rounded))]),body:FutureBuilder<UserAnalytics>(future:future,builder:(_,s){if(s.connectionState!=ConnectionState.done)return const Center(child:CircularProgressIndicator());if(s.hasError)return Center(child:Text('${s.error}'));final a=s.data!;final level=a.approved>=100?5:a.approved>=50?4:a.approved>=25?3:a.approved>=10?2:1;final next=level==1?10:level==2?25:level==3?50:level==4?100:100;final progress=level==5?1.0:(a.approved/next).clamp(0.0,1.0);final badges=[_B('First Step','Complete your first approved task.',a.approved>=1,Icons.flag_rounded),_B('Task Hunter','Reach 10 approved tasks.',a.approved>=10,Icons.search_rounded),_B('Rising Star','Reach 25 approved tasks.',a.approved>=25,Icons.auto_awesome_rounded),_B('Pro Worker','Reach 50 approved tasks.',a.approved>=50,Icons.workspace_premium_rounded),_B('Elite','Reach 100 approved tasks.',a.approved>=100,Icons.military_tech_rounded),_B('7-Day Fire','Maintain a 7-day activity streak.',a.streak>=7,Icons.local_fire_department_rounded)];return ListView(padding:const EdgeInsets.fromLTRB(16,10,16,30),children:[Container(padding:const EdgeInsets.all(22),decoration:BoxDecoration(gradient:const LinearGradient(colors:[Color(0xFF111827),Color(0xFF7C3AED)]),borderRadius:BorderRadius.circular(28)),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Row(children:[const Icon(Icons.workspace_premium_rounded,color:Colors.white,size:34),const SizedBox(width:10),Text('Level $level',style:const TextStyle(color:Colors.white,fontSize:27,fontWeight:FontWeight.w900))]),const SizedBox(height:8),Text(level==5?'Maximum milestone reached':'${a.approved} approved • next milestone $next',style:const TextStyle(color:Colors.white70)),const SizedBox(height:14),ClipRRect(borderRadius:BorderRadius.circular(20),child:LinearProgressIndicator(value:progress,minHeight:9,backgroundColor:Colors.white12,valueColor:const AlwaysStoppedAnimation<Color>(Colors.white))) ]),const SizedBox(height:20),const Text('Achievements',style:TextStyle(fontSize:20,fontWeight:FontWeight.w900)),const SizedBox(height:10),...badges.map((b)=>Container(margin:const EdgeInsets.only(bottom:10),padding:const EdgeInsets.all(15),decoration:BoxDecoration(color:Theme.of(c).colorScheme.surface,borderRadius:BorderRadius.circular(20),border:Border.all(color:Theme.of(c).dividerColor.withOpacity(.5))),child:Row(children:[Container(width:46,height:46,decoration:BoxDecoration(color:b.unlocked?Theme.of(c).colorScheme.primary.withOpacity(.12):Colors.grey.withOpacity(.1),borderRadius:BorderRadius.circular(14)),child:Icon(b.icon,color:b.unlocked?Theme.of(c).colorScheme.primary:Colors.grey)),const SizedBox(width:12),Expanded(child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text(b.title,style:const TextStyle(fontWeight:FontWeight.w900)),const SizedBox(height:3),Text(b.description,style:TextStyle(fontSize:12,color:Theme.of(c).colorScheme.onSurface.withOpacity(.62)))])),Icon(b.unlocked?Icons.check_circle_rounded:Icons.lock_outline_rounded,color:b.unlocked?Colors.green:Colors.grey)])))]);});}
}
class _B{final String title,description;final bool unlocked;final IconData icon;const _B(this.title,this.description,this.unlocked,this.icon);}
